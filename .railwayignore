# Local Node.js binaries — must not be sent to Railway build context
node-v22.11.0-win-x64/
node-v22.11.0-win-x64.zip

# Local Android/JDK toolchains
.tools/

# Build artifacts
build/
dist/
android/
ios/
apk-backups/
*.apk
*.aab
Breeding-planner-project.zip

# Backup/scratch files with potentially invalid encoding
App_prev.jsx
HEAD_App.jsx
main_App.jsx

# Windows-generated lockfiles — npm has a known bug (npm/cli#4828) where installing
# from a lockfile generated on a different platform fails to pull the correct native
# binary for packages like @tailwindcss/oxide, even though the lockfile lists it.
# Without the lockfile, Nixpacks falls back to `npm install`, which resolves platform
# deps fresh and correctly.
/package-lock.json
breeding-app-breeder/package-lock.json

# Dependencies
node_modules/
*/node_modules/

# Dev / test artifacts
.playwright/
playwright-report/
test-results/
coverage/

# IDE / OS
.claude/
.vscode/
*.log
.DS_Store
Thumbs.db
