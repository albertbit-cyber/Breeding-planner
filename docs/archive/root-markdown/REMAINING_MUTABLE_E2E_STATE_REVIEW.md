# Remaining Mutable E2E State Review

Step: 224

## Findings

- Timestamp-based values remain in these E2E areas:
  - Lab result codes.
  - Lab temporary breeder-created animal ID.
  - Breeder certificate result code.
- Browser auth storage state is generated by Playwright setup projects.
- Dynamic lab orders created by tests are owned by the seeded breeder and cleaned by reset.

## Risk

Low for local deterministic suite runs because `test:e2e:reset` clears seeded breeder lab orders before execution.

## Recommended Follow-Up

Replace timestamped values with test-title-derived deterministic suffixes once the larger E2E suite stabilizes.
