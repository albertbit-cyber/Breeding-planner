# APK Change Log

This is the required shared APK change log for Claude and Codex.

Every APK edit, rebuild, replacement, rename, copy into `apk-backups/`, or recommendation of the latest installable APK must be recorded here before the user is told which APK to install.

## Required Entry Format

| Date/time | Agent | APK | Type | Changes | SHA256 | Verification |
| --- | --- | --- | --- | --- | --- | --- |

Use Europe/Berlin local time with UTC offset, for example `2026-07-06 00:52 +02:00`.

## Entries

| Date/time | Agent | APK | Type | Changes | SHA256 | Verification |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-07-27 12:38 +02:00 | Claude | `app-debug-2026-07-27-serpentora-logo-splash.apk` | Debug | First build to verify the real Serpentora ouroboros logo (replacing "PLACEHOLDER" art) on-device: PWA/app icons, Android launcher icons, and native splash screens. Built from `breeding-app-breeder/` (the actively developed app) — **discovered this session that the repo has two separate, divergent Capacitor Android projects sharing the same appId `com.breedingplanner.mobile`**: this one (`breeding-app-breeder/android/`) and a stale legacy one at the repo root (`android/`, still named "Breeding Planner Mobile", untouched since 2026-07-03). All prior APKs in this log/inventory were built from the **root** project via `npm run android:release:apk` at repo root, not from `breeding-app-breeder/`. Since they share an appId, installing either one over the other silently replaces it on-device — this is why the new logo didn't appear after the user rebuilt/reinstalled via the old root pipeline. Root's `scripts/android-build.ps1` was never repointed at `breeding-app-breeder` (known since the 2026-07-09 consolidation, see `[[project_lab_mobile_app]]`/apk-workflow memory); this build instead ran `npm run build` + `npx cap sync android` + `gradlew assembleDebug` directly inside `breeding-app-breeder/`, bypassing the copied-but-unwired `breeding-app-breeder/scripts/android-build.ps1` (which still calls nonexistent `build:android:dev/staging/prod` npm scripts). Root `android/` was left untouched. Build also hit a transient Windows page-file exhaustion (JVM mmap failure, unrelated to the project) that resolved on retry with no lasting config changes. | `17B3153111F7E293885DE41FCB8392F91AF2DD0B669CCAA09D628EB479D1ACA6` | Confirmed generated APK SHA256 matches archived copy. Static-only — no physical device/emulator connected in this environment, so the splash/icon/login-logo appearance is still unverified on-device; user needs to install and confirm. Package/appId not independently re-verified beyond source inspection (no `aapt dump badging` run). |
| 2026-07-10 20:52 +02:00 | Claude | `app-debug-2026-07-10-lab-staging-sampleid-fix.apk` | Debug | Two fixes: (1) Sample IDs/QR codes now derive from the human-readable order number instead of the internal database ID — `07AA00001` → samples `07AA00001-1`, `07AA00001-2`, etc. (previously e.g. `clxyz9f8a-sample-1`). Fixed in both `breeding-app-lab` and `breeding-app-breeder`'s `features/lab/api/client.ts` (`getSharedSampleId` + the `sampleIds` array in `toLegacyOrder`, both apps independently recompute these since there's no persisted `Sample` table) and the sample-ID validation regex in both apps' `qrLookupInput.ts`. Note: labels already printed under the old `-sample-N` scheme will no longer resolve after this change. (2) Diagnosed why submitted test orders weren't showing in the mobile dashboard: confirmed via `https://breeding-planner.netlify.app/`'s bundled JS that the web app the user tests against is pointed at the **staging** backend (`breeding-planner-staging.up.railway.app`), but the mobile APK was built against **production** — different databases entirely, not a query/logic bug. Added `breeding-app-lab/.env.android-staging` and rebuilt against staging. | `EF333AB95F64BE4A319E10534CD16076C143D99DE11328D182CD435C5255BEEA` | Verified build output SHA256 matches archived copy, confirmed the staging URL (not production) is baked into the packaged JS inside the APK, and confirmed the bundled `index.html` references the newly rebuilt JS chunk. Static-only — no physical device/emulator available in this environment; the sample-ID format and dashboard order visibility still need to be confirmed on an actual phone against staging. Supersedes `app-debug-2026-07-10-lab-bottom-nav-scan-routing.apk` for day-to-day testing (that one is still valid against production, just not what the user's test data is in). |
| 2026-07-10 19:12 +02:00 | Claude | `app-debug-2026-07-10-lab-bottom-nav-scan-routing.apk` | Debug | Requested UX redesign: replaced the mobile hamburger drawer with a persistent bottom tab bar (`LabAppShell.jsx`) — Dashboard / Orders / **Scan** (center, prominent) / Results / More — so the QR scanner is reachable from every screen, not just the Sample Intake page. Added `GlobalScanOverlay.jsx`: a full-screen camera scan reachable from the bottom bar that resolves the scanned sample's *current* order status via the existing `resolveLabSampleByQrToken` API and routes automatically — `submitted` → Sample Intake (pre-resolved, no re-scan needed, via new `presetToken` prop/`?token=` query param); `received`/`in_progress` → Result Entry pre-loaded to that order (new `presetOrderId` prop/`?orderId=` query param, skips the manual dropdown search); `completed`/`cancelled` → the existing Order Details page as a read-only view. `LabQrScanner.jsx` gained an `autoStart` prop so the overlay's camera opens immediately instead of requiring an extra tap. Result submission already synced genetics back to the breeder's animal record via existing code — no changes needed there, only routing into that same existing flow. | `27B4803656E15B29ED144EFC500E9FE749B0DE6E7D66A43D0655C34576F4B807` | Verified build output SHA256 matches archived copy, and that the bundled `index.html` inside the APK references the newly rebuilt JS chunk. Static-only — no physical device/emulator was available in this environment; the bottom bar, scan-then-auto-route behavior, and both preset-prop paths (intake token, result-entry orderId) still need to be clicked through on an actual phone. Supersedes `app-debug-2026-07-10-lab-mobile-nav-scan-fix.apk`. |
| 2026-07-10 17:32 +02:00 | Claude | `app-debug-2026-07-10-lab-mobile-nav-scan-fix.apk` | Debug | Fixes reported after installing the first lab-app build: (1) QR scanning was unreachable — `LabAppShell.jsx`'s entire nav sidebar was `hidden ... lg:block`, so on any phone-width screen every page except Dashboard (including Sample Intake, which holds the QR scanner) had no way to navigate to it. Added a mobile hamburger-menu drawer exposing the same nav list. (2) The dashboard's per-queue order table (`LabOrderQueueWidget.jsx`) was `min-w-[820px]`, always wider than a phone screen, forcing horizontal scrolling to see all columns — added a stacked-card layout for narrow screens (table still used at `sm:` and up). Also found and fixed the same horizontal-scroll problem in the Order Details page's 6-step workflow progress bar (`OrderDetailsPage.jsx`, was `overflow-x-auto`) — now a 3-column grid on narrow screens, horizontal stepper from `sm:` up. Supersedes `app-debug-2026-07-10-lab-mobile-app.apk`. | `CA2E7503CC96D6713B7B41C5A0D72C9B1F606BBA0897451F3D9FBD0ED604C96B` | Verified build output SHA256 matches archived copy, and that the bundled `index.html` inside the APK references the newly rebuilt JS chunk (not the previous build's). Static-only — no physical device/emulator was available in this environment, so the nav drawer and QR scanner still need to be clicked through on an actual phone to confirm. |
| 2026-07-10 02:48 +02:00 | Claude | `app-debug-2026-07-10-lab-mobile-app.apk` | Debug | New companion app for the Laboratory portal (`breeding-app-lab`), package `com.breedingplanner.lab` — distinct from the breeder mobile app (`com.breedingplanner.mobile`) tracked above, can be installed side-by-side. Wraps the existing `breeding-app-lab` React app (same Incoming Orders / Sample Intake QR scan / Result Entry / Completed Tests workflow as the browser portal, unchanged) in a fresh self-contained Capacitor Android project, since the repo's root Android build pipeline was left broken by yesterday's source-consolidation cleanup (root `src/` was removed). Also ported the breeder app's bearer-token-first auth fix (`e4be5d3`) into `breeding-app-lab/src/shared/apiClient.ts`, since cookie-preferred auth is unreliable in a native WebView / cross-origin context. Built against production API (`https://breeding-planner-production.up.railway.app/api`). | `5C96B84421BA1B1293A2502826F6EA17EF04658F40435C8992CC9234C3ED5EF1` | Verified build output SHA256 matches archived copy. Confirmed merged AndroidManifest has `INTERNET`/`ACCESS_NETWORK_STATE`/`CAMERA` permissions and correct package `com.breedingplanner.lab`. Confirmed production `VITE_API_URL` is baked into the built JS bundle. Static-only checks — no physical device/emulator was available in this environment to install and click through the app, so on-device login/QR-scan/result-entry flows are unverified. Known caveat: the app's CSS loads via the Tailwind CDN `<script>` tag at runtime (same as the browser portal), so first launch needs network access before styling renders — not a new regression vs. the web app, but worth moving to a build-time Tailwind setup (like `breeding-app-breeder` already has) in a follow-up. Debug-signed only; no release keystore configured for this app yet. |
| 2026-07-09 01:35 +02:00 | Codex | `app-release-2026-07-09-mobile-cloud-animal-sync.apk` | Release | Restored the mobile stylesheet and native Android entry point lost during source consolidation; preserved the last successful account-specific snapshot; stopped converting cloud read failures into empty animal lists; exposed the real cloud error with retry guidance; and prevented animal/pairing writes from merging against a fabricated empty snapshot. | `447C7EB55D954A73A2D320EE04BF9CE48768A1FF6E60477E4E24846D63EA9969` | Production Android web bundle and signed release APK built successfully. Confirmed mobile CSS is present, production API URL is baked into the Android build, package is `com.breedingplanner.mobile`, min/target SDK are 24/36, APK v2 signature is valid, signer matches the July 7 APK, and generated/archive hashes match. Canonical breeder typecheck was attempted but remains blocked by pre-existing missing lab handler modules and one missing inline image type. |
| 2026-07-07 14:05 +02:00 | Codex | `app-release-2026-07-07-mobile-full-redesign.apk` | Release | Rebuilt the mobile APK after the full mobile redesign: mobile snapshots now preserve planner state, mobile rack/spaces view reads desktop rooms/racks/terrariums, animal profiles include Feed Cycle, full details, all log categories, normalized genetics, settings data summaries, and automatic signed-in/online sync refresh with queued-action upload. | `1D54F0B5EB0D2C039EEEEB044A2EB8BC1D01C869C8D312CB803E38441839CAB6` | Ran root production build, Android production release APK build, verified generated APK hashes matched the archived APK, removed generated duplicate APKs from `dist/android/` and Android Gradle outputs, and re-scanned the workspace so retained APKs are only in `apk-backups/`. Root Vitest suite was attempted but failed on existing suite configuration issues unrelated to this change: backend tests missing `DATABASE_URL`, Playwright e2e specs being collected by Vitest, shared-package missing test imports, and `src/public_url.test.js` missing Vitest globals. |
| 2026-07-06 03:20 +02:00 | Codex | `app-release-2026-07-06-unified-cloud-sync.apk` | Release | Rebuilt the mobile APK with unified cloud sync fixes: backend nested animal/pairing merge preservation, owner planner-state sync for racks/rooms/groups/feed defaults/appearance, mobile save paths that merge with the latest cloud snapshot before uploading, and first-sync protection so a fresh device cannot overwrite existing cloud planner state with defaults. | `BA4194036827CF5E50394725DECA5E46515578D8879D79DA1DC69FF2C2327515` | Ran backend sync tests, backend build, root app build, breeder app build, Android release APK build, verified generated APK hashes matched the archived APK, and re-scanned the workspace so retained APKs are only in `apk-backups/`. |
| 2026-07-06 00:57 +02:00 | Codex | `apk-backups/*.apk` | APK folder cleanup | Made `apk-backups/` the only retained APK folder. Removed duplicate APK copies from `android/app/build/outputs/apk/`, `dist/android/`, and `node_modules/.cache/gh-pages/.../apk-backups/` after verifying each duplicate hash matched a file already in `apk-backups/`. Updated handoff docs so future APK builds remove duplicate generated/cache copies after archiving. | Multiple, unchanged from inventory rows in `CLAUDE_APK_HANDOFF.md` | Re-scanned the workspace for `*.apk`; only eight APK files remain, all under `apk-backups/`. |
| 2026-07-06 00:52 +02:00 | Codex | `app-release-2026-07-05-demo-snakes-local-only.apk` | Release | Confirmed as latest installable APK from the current archive state; includes cloud sync convergence and demo-snakes-local-only work. Added this mandatory shared APK logging rule for future Codex and Claude APK work. | `CCA6FBFE3C9AE317EB472176E70672140F4A6098C61053F49A4FBA7526312C15` | Compared workspace APK timestamps and SHA256 hashes. No APK rebuild performed. |
